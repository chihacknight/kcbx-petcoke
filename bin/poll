#!/usr/bin/env node
var cwd = process.cwd();
require(cwd + "/lib/env");

var weather = require(cwd + "/lib/weatherService")
  , qryLat = 41.7
  , qryLng = -87.55
  ;

weather.getForecast(qryLat, qryLng, function(err, fc){
  process.exit();
})